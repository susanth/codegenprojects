
@Test
public void testCalculateTotal() {
    List<Product> products = new ArrayList<>();
    products.add(new Product("Eraser", 10, 5));
    products.add(new Product("Pencil", 20, 10));
int total = cartService.calculateTotal(products);
    assertEquals(310, total);
}